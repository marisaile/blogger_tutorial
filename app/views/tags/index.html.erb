<h1>All Tags</h1>
<hr class="separator">
<ul class="tag_index">
	<% @tags.each do |tag| %>
		<li><%= link_to tag.name, tag_path(tag), class: 'tag_destination', id: "tag_#{tag.id}" %></li>
	<% end %>
</ul>
