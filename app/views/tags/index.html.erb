<h1>All Tags</h1>
<ul id="tag">
	<% @tags.each do |tag| %>
		<li><%= link_to tag.name, tag_path(tag), class: 'tag_name', id: "tag_#{tag.id}" %><br />
			<%= link_to 'delete', tag_path(tag), method: :delete, data: {confirm: "Are you sure?"} %>
		</li>
	<% end %>
</ul>
